module.exports = function (options) {

	options.lessPath = options.lessPath || '/style';
	options.staticPath = options.staticPath || '/public';
	options.scriptPath = options.scriptPath || '/script';
	options.libPath = options.libPath || '/lib';
	options.initPath = options.initPath || '/init';

};